<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET"></uses-permission>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.CollegeSystem"
        tools:targetApi="31" >
        <activity
            android:name=".login"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".signUpLecturer"></activity>
        <activity android:name=".signUpStudent"></activity>
        <activity android:name=".homePage"></activity>
        <activity android:name=".toDoList"></activity>
        <activity android:name=".addToDoList"></activity>
        <activity android:name="subjectRegistration"></activity>
        <activity android:name=".chooseSubject"></activity>
        <activity android:name=".addStudentToSubject"></activity>
        <activity android:name=".studentAttendance"></activity>
        <activity android:name=".lecturerSubject"></activity>
        <activity android:name=".adminPage"></activity>
        <activity android:name=".updateToDo"></activity>
    </application>

</manifest>